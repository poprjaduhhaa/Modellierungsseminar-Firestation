# TODO Артём (2026-07-07, после большого апдейта Дирка)

Тикеты и детали: `coding/AUDIT.md` (версия 2, полностью переписан под новый код).

## Статус
- Мейн Дирка влит в Arty-Workspace и запушен (HEAD bc8c2b6). Конфликтов не было.
- Дирк сам нашёл и починил коллапс со свободными днями (фильтр WorkShifts).
- ВАЖНО: ноутбук сейчас НЕ ЗАПУСТИТСЯ у тебя: в ячейке вывода вложенные двойные
  кавычки в f-строке (SyntaxError на Python 3.10, у Дирка версия новее) плюс
  вывод пишется в два разных файла (output_cycle_.csv и output_cycle.csv).

## Что делать, по порядку
1. S0: починить ячейку вывода (кавычки + имя файла). Первый тикет, без него
   никакие прогоны невозможны. Проси ассистента дать блок.
2. Отправить команде 4 вопроса (список внизу AUDIT.md, раздел 5, Team questions).
3. Дальше тикеты по одному: S1 датасет Pesch+ -> S2 reserve 24ч в часы ->
   S3 c06 = 56ч -> S4 багфиксы Pesch8 (сначала пингануть Дирка).
4. Потом S5 Pesch3 (разблокирован: минимизация смен shiftID день в день,
   общий механизм с Pesch8, не дублировать код).
5. Коммит после каждого зелёного прогона, пуш сразу (Дирк работает параллельно).

## Правило после каждого pull
Номера ячеек уезжают. Прежде чем править по старым подсказкам, попроси
ассистента передампить ячейки и сверить якоря.
